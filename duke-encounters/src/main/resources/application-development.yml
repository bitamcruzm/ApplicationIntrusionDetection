# Session
server:
  session:
    timeout: 3600

# Actuator
management:
  context-path: /admin
  security:
    enabled: false

security:
  basic:
    enabled: false

info:
  application:
    env: Development